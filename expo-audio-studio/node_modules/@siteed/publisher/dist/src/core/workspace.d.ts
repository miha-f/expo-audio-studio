import type { PackageJson } from "type-fest";
import type { MonorepoConfig, PackageContext, ReleaseConfig } from "../types/config";
import { Logger } from "../utils/logger";
export declare class WorkspaceService {
    private readonly logger;
    private packageCache;
    private rootDir;
    private configPromise;
    constructor(config?: MonorepoConfig, logger?: Logger);
    getRootDir(): string;
    getPackages(packageNames?: string[]): Promise<PackageContext[]>;
    private matchPackageName;
    getChangedPackages(): Promise<PackageContext[]>;
    getPackageConfig(packageName: string): Promise<ReleaseConfig>;
    readPackageJson(packagePath: string): Promise<PackageJson>;
    private getWorkspaceGlobs;
    private ensureStringRecord;
    writePackageJson(packagePath: string, data: PackageJson): Promise<void>;
    getCurrentPackage(): Promise<PackageContext | null>;
}
