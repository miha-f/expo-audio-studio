import { PackageManagerService } from "./package-manager";
import { Logger } from "../utils/logger";
import type { DependencyValidationReport } from "../types/config";
export declare class WorkspaceIntegrityService {
    private packageManager;
    private logger;
    constructor(packageManager: PackageManagerService, logger: Logger);
    check(): Promise<boolean>;
    checkWithDetails(verbose?: boolean): Promise<DependencyValidationReport>;
    fix(): Promise<boolean>;
}
