import { GenerateFileFormat } from "../templates";
import { Logger } from "../utils/logger";
import { WorkspaceService } from "./workspace";
export interface InitOptions {
    force?: boolean;
    interactive?: boolean;
    format?: GenerateFileFormat["format"];
}
interface InitializeParams {
    packages: string[];
    options?: InitOptions;
}
export declare class InitService {
    private logger;
    private workspaceService;
    private packageManager;
    constructor(logger: Logger, workspaceService?: WorkspaceService);
    initialize({ packages, options, }: InitializeParams): Promise<void>;
    private promptForPackageOptions;
    private promptForMonorepoOptions;
    private validateNoExistingConfig;
    private initializePackageFiles;
    private initializeRootConfig;
    private createDirectoryStructure;
}
export {};
