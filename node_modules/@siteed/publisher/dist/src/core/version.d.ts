import type { BumpType, GitConfig, PackageContext, ReleaseConfig } from "../types/config";
export declare class VersionService {
    private readonly git;
    constructor(gitConfig: GitConfig);
    private execCommand;
    bump(context: PackageContext, config: ReleaseConfig): Promise<void>;
    determineVersion(context: PackageContext, bumpType: BumpType, preReleaseId?: string): string;
    updateDependencies(context: PackageContext, updatedPackages: Map<string, string>): Promise<void>;
    analyzeCommits(context: PackageContext): Promise<BumpType>;
    validateVersion(version: string): void;
}
