export interface FormatTagOptions {
    packageName: string;
    version: string;
    tagPrefix: string;
}
export declare const formatGitTag: ({ packageName, version, tagPrefix, }: FormatTagOptions) => string;
