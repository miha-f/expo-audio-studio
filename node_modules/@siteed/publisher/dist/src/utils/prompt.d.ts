import { VersionService } from "../core/version";
import type { BumpType, PackageContext } from "../types/config";
import { Logger } from "./logger";
export declare class Prompts {
    private logger;
    constructor(logger: Logger);
    getVersionBump(context: PackageContext, versionService: VersionService): Promise<BumpType>;
    confirmRelease(): Promise<boolean>;
    selectPackages(availablePackages: string[]): Promise<string[]>;
    confirmWorkingDirectory(): Promise<boolean>;
    confirmChangelogCreation(packageName: string): Promise<boolean>;
    confirmTagOverwrite(packageName: string, version: string): Promise<boolean>;
    confirmChangelogContent(preview: string): Promise<boolean>;
    getManualChangelogEntry(currentChanges?: string): Promise<string>;
    confirmDependencyUpdates(packageName: string, dependencies: Array<{
        name: string;
        currentVersion: string;
        newVersion: string;
    }>): Promise<boolean>;
}
